# Playwright Test Environment
# Copy this to .env.test and fill in real values

# Base URL of your deployed app
PLAYWRIGHT_BASE_URL=https://tenkara-inbox-nine.vercel.app

# Test user credentials (create a test user in Settings → Team Members)
TEST_USER_EMAIL=rod@test.com
TEST_USER_PASSWORD=testpassword123

# Admin user credentials (for admin-only tests)
TEST_ADMIN_EMAIL=rod@test.com
TEST_ADMIN_PASSWORD=testpassword123
